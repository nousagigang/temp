name: Import Static Folder

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import-static:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout migration branch
        uses: actions/checkout@v4
        with:
          ref: migration

      - name: Clone source repo
        run: git clone --depth 1 https://github.com/k4lm3d/nsg.git source

      - name: Replace static folder
        run: |
          rm -rf static
          cp -R source/static ./static

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add static

          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Import static folder from k4lm3d/nsg"
            git push origin HEAD:migration
          fi
